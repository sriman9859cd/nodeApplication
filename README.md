# nodeApplication
node-version
# nodemon
Usage of nodemon is to run the code automatically without start everytime manually.It is a development tool.Hence it is devDependency.
# express
Expresss is a runtime tool (dependency).
# dotenv
To keep sensitive data and configs separate from code.

